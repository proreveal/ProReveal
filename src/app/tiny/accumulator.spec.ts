import { MinAccumulator, MaxAccumulator, SumAccumulator,
    CountAccumulator, MeanAccumulator }
    from './accumulator';

describe('accumulator test', () => {
    it('should compute min', () => {
        // let init = MinAccumulator.initPartialValue;

        expect(1).toBe(1);
    })
});
